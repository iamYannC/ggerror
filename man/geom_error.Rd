% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-error.R
\docType{data}
\name{geom_error}
\alias{geom_error}
\alias{geom_error_linerange}
\alias{geom_error_crossbar}
\alias{geom_error_pointrange}
\alias{GeomError}
\title{Error bars with automatic orientation}
\usage{
geom_error(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  error_geom = "errorbar",
  orientation = NA,
  sign_aware = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_error_linerange(..., error_geom)

geom_error_crossbar(..., error_geom)

geom_error_pointrange(..., error_geom)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{The data to be displayed in this layer.}

\item{stat}{The statistical transformation to use on the data. Defaults
to \code{"identity"}.}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{error_geom}{One of \code{"errorbar"} (default), \code{"linerange"},
\code{"crossbar"}, or \code{"pointrange"}. Chooses which ggplot2 range geom
\code{geom_error()} dispatches to under the hood.}

\item{orientation}{Either \code{NA} (the default; inferred from the data),
\code{"x"} (vertical error), or \code{"y"} (horizontal error).}

\item{sign_aware}{If \code{TRUE}, signed values in \code{error} are routed per
row: positive values extend the bar in the positive direction,
negative values extend it in the negative direction, and the
opposite side is suppressed. Useful for residual plots where
\code{x}/\code{y} is the fitted value and the bar extends toward the observed
value. Incompatible with \code{stat = "error"}. Default \code{FALSE}.}

\item{na.rm}{If \code{FALSE}, missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\description{
A thin wrapper around \code{\link[ggplot2:geom_linerange]{ggplot2::geom_errorbar()}},
\code{\link[ggplot2:geom_linerange]{ggplot2::geom_linerange()}}, \code{\link[ggplot2:geom_linerange]{ggplot2::geom_crossbar()}}, and
\code{\link[ggplot2:geom_linerange]{ggplot2::geom_pointrange()}} that accepts a single \code{error} aesthetic
and figures out orientation from the data. For asymmetric errors, use
\code{error_neg} + \code{error_pos} instead of \code{error}.
}
\section{Niche parameters (via \code{...})}{

\itemize{
\item \code{zero_threshold} — numeric tolerance (default \code{1e-8}) for the
uniformly-zero check that drives the \code{0 -> NA} deprecation.
\item \code{silent_zero_warning} — \code{TRUE} suppresses that deprecation.
}
}

\section{Aesthetics}{

\code{geom_error()} requires \code{x}, \code{y}, and one of:
\itemize{
\item \code{error} — symmetric half-width applied along the non-categorical axis.
\item \code{error_neg} \strong{and} \code{error_pos} — asymmetric; the bar extends
\code{error_neg} in the negative direction and \code{error_pos} in the positive
direction along the non-categorical axis. For a one-sided bar, set
the unused side to \code{NA} — the cap, stem, and shared-bound cap on
that side are all suppressed.
}

Mixing \code{error} with \code{error_neg} / \code{error_pos} is an error, as is
providing only one of the asymmetric pair.

Fixed per-side styling can be supplied through \code{...} with \verb{_neg} and
\verb{_pos} suffixes for \code{colour}, \code{fill}, \code{linewidth}, \code{linetype}, \code{alpha},
and \code{width}.
These are fixed scalar parameters, not mapped aesthetics.
}

\examples{
library(ggplot2)

ggplot(mtcars, aes(mpg, rownames(mtcars))) +
  geom_point() +
  geom_error(aes(error = drat))

ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_point() +
  geom_error(aes(error = drat), error_geom = "pointrange")

# Asymmetric: bar extends drat/2 below and drat above each point
ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_point() +
  geom_error(aes(error_neg = drat / 2, error_pos = drat))

# Style the negative and positive halves separately
ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_point() +
  geom_error(
    aes(error_neg = drat / 2, error_pos = drat),
    colour_neg = "steelblue",
    colour_pos = "firebrick"
  )

}
\keyword{datasets}
