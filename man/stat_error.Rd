% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-error.R
\docType{data}
\name{stat_error}
\alias{stat_error}
\alias{StatError}
\alias{GeomErrorStat}
\title{Summarising stat for \code{geom_error()}}
\usage{
stat_error(
  mapping = NULL,
  data = NULL,
  geom = NULL,
  position = "identity",
  ...,
  fun = "mean_se",
  fun.args = list(),
  error_geom = "errorbar",
  orientation = NA,
  na.rm = FALSE,
  conf.int = 0.95,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, position, show.legend, inherit.aes}{Standard ggplot2
layer arguments.}

\item{geom}{The geom to render the summary with. Defaults to
\link{GeomErrorStat}, which reuses \code{geom_error()}'s draw path.}

\item{...}{Additional parameters. Names that match \code{fun}'s formals (or
any name, when \code{fun} accepts \code{...}) are forwarded to \code{fun}; the
remainder go to \code{\link[=geom_error]{geom_error()}} as per-side styling (\code{colour_neg},
\code{width_pos}, …) or standard aesthetics.}

\item{fun}{One of \code{"mean_se"} (default, uses \code{\link[ggplot2:mean_se]{ggplot2::mean_se()}}),
\code{"mean_ci"} (mean with 95\% normal-theory CI via \code{\link[stats:TDist]{stats::qt()}}; no
Hmisc dependency), or a function taking a numeric vector and returning
a single-row data.frame with columns \code{y}, \code{ymin}, \code{ymax}.}

\item{fun.args}{Named list of extra arguments to pass to \code{fun}. Merged
with any \code{...} arguments whose names match \code{fun}'s formals; \code{fun.args}
wins on collision.}

\item{error_geom}{One of \code{"errorbar"} (default), \code{"linerange"},
\code{"crossbar"}, \code{"pointrange"}.}

\item{orientation}{\code{NA} (default, inferred), \code{"x"}, or \code{"y"}.}

\item{na.rm}{If \code{TRUE}, drop \code{NA} values from the summarised axis before
applying \code{fun}.}

\item{conf.int}{Confidence level forwarded to \code{fun} when the function
accepts a \code{conf.int} argument (e.g. \code{fun = "mean_ci"} or a custom
\code{fun.data} with that formal). Ignored for funs that don't declare it,
so it's safe to leave at the default when using \code{fun = "mean_se"}.}
}
\description{
\code{stat_error()} computes the error bounds from raw observation-level data
using ggplot2's \code{fun.data} contract. Where \code{\link[=geom_error]{geom_error()}} expects pre-
computed error columns, \code{stat_error()} summarises \code{y} (or \code{x}, when
orientation is horizontal) within each group via the function supplied to
\code{fun}.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(factor(cyl), mpg)) + stat_error()

ggplot(mtcars, aes(factor(cyl), mpg)) +
  stat_error(fun = "mean_ci", error_geom = "pointrange")

# 90\% CI with NA-tolerant summarising:
ggplot(mtcars, aes(factor(cyl), mpg)) +
  stat_error(fun = "mean_ci", conf.int = 0.9, na.rm = TRUE)

}
\keyword{datasets}
